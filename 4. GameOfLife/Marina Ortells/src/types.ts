export {};

export type Cell = {
    isAlive: boolean;
};


export type State = Cell[][];





/**
 * 
 * empezar por GameofLIfe que no depende de nada
 * - que la clase GOL funcione
 * - comprobar que funcione el input usuario
 * - dibujar grid y que funcione dubujar cell y borrar
 * - currentState sea lo que le hayas dibujado
 *      GOL -> inicializar cada vez o reset
 * 
 * //console.table
 * 
 * PREGUNTAS:
 *
 * - si pongo Cell[][], como hago para referirme a las propiedades de width, height...
 * - como hago para llamar a la variable row y columns en initialState, en gameoflife??
 *
 *
 * preguntas para más tarde:
 * - como hago para dibujar el grid otra vez?
 *
 *
 *
 */
